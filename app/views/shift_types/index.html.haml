- content_for :title, 'Shift Types'

%h2.page-header Shift Types

%p= link_to "Create New Shift Type", new_shift_type_path

.panel.panel-default
  .table-responsive
    %table.table.table-striped
      %tbody
        %tr
          %th Name
          %th Description
          %th Address
          %th Required Role
          %th
          %th
        - @shift_types.each do |type|
          %tr
            %td= type.name
            %td= type.description
            %td= type.address.line_1
            %td= type.role.present? ? type.role.name : 'N/A'
            %td
              = link_to edit_shift_type_path(type), class: 'header-icon-right' do
                %i.fa.fa-pencil.fa-lg{"data-placement" => "right", "data-toggle" => "tooltip", :title => ""}
            %td
              = link_to shift_type_path(type), method: :delete, data: { confirm: "Are you sure you want to remove the #{type.name} shift type?" }, class: 'header-icon-right' do
                %i.fa.fa-trash.fa-lg{"data-placement" => "right", "data-toggle" => "tooltip", :title => ""}
